import { ClearOnEscapeDirective } from './clear-on-escape.directive';

describe('ClearOnEscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearOnEscapeDirective();
    expect(directive).toBeTruthy();
  });
});
